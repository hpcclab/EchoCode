# UserImplementation for ChatBot

Set `echocode.featureImplementation.chatBot` to `user` to enable overrides.

Supported override files in this folder:

- `Chat_Tutor.js` exports `registerChatCommands`

If the file is missing or exports are invalid, EchoCode falls back to built-in behavior.
