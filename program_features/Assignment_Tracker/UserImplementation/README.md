# UserImplementation for Assignment_Tracker

Set `echocode.featureImplementation.assignmentTracker` to `user` to enable overrides.

Supported override files in this folder:

- `assignmentTracker.js` exports `registerAssignmentTrackerCommands`

If the file is missing or exports are invalid, EchoCode falls back to built-in behavior.
