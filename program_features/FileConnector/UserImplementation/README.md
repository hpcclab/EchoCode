# UserImplementation for FileConnector

Set `echocode.featureImplementation.fileConnector` to `user` to enable overrides.

Supported override files in this folder:

- `File_Connector.js` exports `registerFileConnectorCommands`

If the file is missing or exports are invalid, EchoCode falls back to built-in behavior.
